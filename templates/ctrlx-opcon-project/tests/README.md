# 测试

- `static/`：目录、规格、对象归属、ST 风格、SetEvent 长度和禁用操作检查；
- `compile/`：离线编译及 warning 签名基线；
- `simulation/`：不接实体 PLC 的 FB/Chain 场景。
- `ioe/`：ASC-only 电气输入、canonical CSV 和 CpStudio ASC 往返检查。

实体 PLC 的连接、下载、启停和 FORCE 不属于自动测试。
